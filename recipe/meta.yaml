{% set name = "wagtail-footnotes" %}
{% set version = "0.14.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/wagtail_footnotes-{{ version }}.tar.gz
  sha256: 6355b396799537154b9c8bbaa54a800d82f58b345f87e987b32693f92d9c3f9d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - flit-core >=3.2,<4
    - pip
  run:
    - python >={{ python_min }}
    - wagtail >=6.3
    - django >=4.2

test:
  imports:
    - wagtail_footnotes
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: Add footnotes to rich text in your Wagtail pages
  home: https://github.com/torchbox/wagtail-footnotes
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
